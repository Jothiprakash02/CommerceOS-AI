# ── Core API ─────────────────────────────────
fastapi>=0.135.0
uvicorn[standard]>=0.41.0
pydantic>=2.12.0

# ── Database ─────────────────────────────────
sqlalchemy>=2.0.48

# ── HTTP ─────────────────────────────────────
requests>=2.32.0
httpx>=0.28.0               # async HTTP client — used by google-genai SDK

# ── Scraping ─────────────────────────────────
beautifulsoup4>=4.13.0
lxml>=6.0.0                 # faster HTML parser for BS4

# ── Google Trends ─────────────────────────────
pytrends==4.9.2

# ── Reddit API (M0 — trend scouting, optional) ────────────────────────────────
praw>=7.8.0                 # Python Reddit API Wrapper
                            # Requires: REDDIT_CLIENT_ID, REDDIT_CLIENT_SECRET

# ── Gemini AI (chat assistant) ────────────────────────────────────────────────
google-genai>=1.0.0         # Google Gemini SDK (replaces deprecated google-generativeai)
                            # Requires: GEMINI_API_KEY
                            # Free key: https://aistudio.google.com/app/apikey
                            # Model: models/gemini-2.0-flash-lite

# ── Data / export ────────────────────────────
pandas>=2.3.0               # CSV export and data manipulation
openpyxl>=3.1.0             # Excel export support (pandas backend)

# ── Frontend static file serving ─────────────
aiofiles>=23.2.0            # required by FastAPI StaticFiles mount

# ── Utilities ────────────────────────────────
python-dotenv>=1.2.0        # load .env file for API keys
